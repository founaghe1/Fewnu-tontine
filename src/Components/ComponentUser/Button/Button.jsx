import React from 'react';
import './button.css';

const Button = ({ libelet }) => {
    
  
    return (
      <button className='btnModif'>
        {libelet}
      </button>
    );
  }

export default Button